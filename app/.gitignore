# Generated by XcodeGen from project.yml — run `xcodegen generate`
*.xcodeproj/
KorpProxy-Info.plist
KorpProxy.entitlements

# Built engine binary staged for dev runs / bundling
.engine-bin/

# Sparkle EdDSA private key (export it as the SPARKLE_PRIVATE_KEY CI secret, then
# keep this local copy out of git). The matching public key lives in project.yml.
.sparkle_private_key.txt

# Release packaging output + transient appcast (the canonical feed lives on
# this repo's gh-pages branch, not here).
dist/
appcast.xml

# Xcode / build artifacts
build/
DerivedData/
*.xcuserstate
.DS_Store
